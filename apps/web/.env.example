# HiveMQ Cloud WebSocket (browser). Copy to .env and fill in values.
# Restart `pnpm dev` after changing .env.

VITE_MQTT_URI=wss://YOUR_CLUSTER.s1.eu.hivemq.cloud:8884/mqtt
VITE_MQTT_USERNAME=
VITE_MQTT_PASSWORD=
# VITE_MQTT_CLIENT_ID=wol_ui_dev
# VITE_MQTT_AUTO_RECONNECT=true
